<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <View
        android:id="@+id/textView"
        android:layout_width="20dp"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:background="@drawable/thorttle_back_nine"
        android:outlineAmbientShadowColor="#000000"

        />

    <RelativeLayout

        android:layout_alignParentTop="true"
        android:layout_marginTop="200dp"
        android:layout_centerHorizontal="true"
        android:id="@+id/tvThrottle"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true">

        <ImageView
            android:id="@+id/tvImg"
            android:layout_width="75dp"
            android:layout_height="75dp"
            android:layout_centerInParent="true"
            android:adjustViewBounds="true"
            android:contentDescription="throttle"
            android:scaleType="fitCenter"
            android:src="@drawable/img_throttle" />

        <TextView
            android:id="@+id/tvText"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@+id/tvImg"
            android:fontFamily="@font/display"
            android:gravity="end"
            android:text=""
            android:textColor="#ffffff"
            android:layout_marginRight="5dp"/>
    </RelativeLayout>
</RelativeLayout>